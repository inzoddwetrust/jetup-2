# MLM Testing Scripts - Живое Тестирование

Набор скриптов для **живого тестирования** MLM системы через реальные Telegram аккаунты.

## 🎯 Концепция

Каждый популятор создает минимальную структуру, где **ТЫ** (⭐️ Зодд или ⭐️ Килл) находишься в нужной позиции для теста конкретного сценария. Тестирование происходит **через Telegram** - делаешь покупки от реального аккаунта, проверяешь результаты в БД.

**Живые аккаунты для тестирования:**
- ⭐️ **Зодд** (5971989877) - основной тестовый аккаунт
- ⭐️ **Килл** (5478046601) - дополнительный тестовый аккаунт
- 👑 **Артем** (526738615) - ROOT (DEFAULT_REFERRER)

**НЕТ тестирования через dummy пользователей!** Ты управляешь только живыми аккаунтами через Telegram.

---

## 📦 Популяторы для живого тестирования

### 1. `populate_test_differential_commission.py`

**Сценарий:** Дифференциальные комиссии с компрессией

**Структура:**
```
ROOT (director, 18%)
  → Dummy1 (❌ inactive) - СЖИМАЕТСЯ
    → Dummy2 (✅ builder, 10%)
      → Dummy3 (❌ inactive) - СЖИМАЕТСЯ
        → ⭐️ ЗОДД (✅ start, 7%) - ТЫ делаешь покупку!
```

**Как тестировать:**
```bash
# 1. Создать структуру
python scripts/populate_test_differential_commission.py

# 2. Посмотреть дерево
python scripts/show_tree.py

# 3. В Telegram от Зодд делаешь покупку на $1000

# 4. Проверяешь результат
python scripts/check_commissions.py --last
```

**Ожидаемый результат:**
- Dummy2 получит ~10% + compression от Dummy1 и Dummy3
- ROOT получит оставшееся до 18%
- Сумма всех комиссий = 18% от покупки

---

### 2. `populate_test_investment_bonus.py`

**Сценарий:** Investment Bonus (кумулятивные бонусы по тирам)

**Структура:**
```
ROOT (director)
  → ⭐️ ЗОДД (start, $25,000) - ТЫ делаешь серию покупок!
```

**Тиры:**
- $1,000 → 5% cumulative
- $5,000 → 10% cumulative
- $10,000 → 15% cumulative
- $20,000 → 20% cumulative

**Как тестировать:**
```bash
# 1. Создать структуру
python scripts/populate_test_investment_bonus.py

# 2. В Telegram от Зодд делаешь покупки ПО ПОРЯДКУ:
#    a) $400 → ожидаешь $0 бонуса
#    b) $700 → ожидаешь ~$55 опционов (достиг $1100, тир 5%)
#    c) $4000 → ожидаешь ~$455 опционов (достиг $5100, тир 10%)

# 3. После каждой покупки проверяешь
python scripts/check_investment_bonus.py --user-id 5971989877
```

**Важно:**
- Бонус выдается ОПЦИОНАМИ, не деньгами
- Баланс не меняется (credit + debit = 0)
- Опционы добавляются в портфель

---

### 3. `populate_test_referral_bonus.py`

**Сценарий:** Referral Bonus (1% опционами для покупок ≥$5000)

**Структура:**
```
ROOT (director)
  → ⭐️ ЗОДД (upline) - ПОЛУЧИТ 1% опционами
    → ⭐️ КИЛЛ (downline) - ДЕЛАЕТ покупку ≥$5000
```

**Как тестировать:**
```bash
# 1. Создать структуру
python scripts/populate_test_referral_bonus.py

# 2. В Telegram от КИЛЛ делаешь покупку ≥$5000

# 3. Проверяешь, что Зодд получил опционы
python scripts/check_commissions.py --last
```

**Ожидаемый результат:**
- Килл делает покупку $5000
- Зодд получает 1% = $50 опционов (авто-покупка)
- Bonus record: `commissionType='referral'`
- ActiveBalance транзакции: +$50, -$50 (net = 0)
- Опционы добавлены в портфель Зодд

---

### 4. `populate_test_50_percent_rule.py`

**Сценарий:** Transfer Bonus (правило 50%)

**Структура:**
```
ROOT (director)
  → Dummy1 (builder) ✅ - ПОЛУЧИТ 50% transfer bonus
    → ⭐️ ЗОДД (start) ✅ - НЕТ активных downline
      → Dummy_Child ✅ - делает покупку (симуляция)
```

**Условие:** Если у пользователя НЕТ активных партнеров в первой линии, его upline получает 50% от его комиссии.

**Как тестировать:**
```bash
# 1. Создать структуру
python scripts/populate_test_50_percent_rule.py

# 2. Симулировать покупку от Dummy_Child
#    (через SQL или специальный скрипт)

# 3. Проверить transfer bonus
python scripts/check_commissions.py --last
```

---

## 🔍 Скрипты проверки результатов

### `check_commissions.py`

Показывает детальную разбивку комиссий по покупке.

```bash
# Проверить последнюю покупку
python scripts/check_commissions.py --last

# Проверить конкретную покупку
python scripts/check_commissions.py --purchase-id 123
```

**Вывод:**
```
Purchase ID: 123
Buyer: Зодд (ID: 5971989877)
Amount: $1000.00

Commission breakdown:
Level 1: Dummy2 ✅ [builder]   10.0% = $100.00
Level 2: ROOT   ✅ [director]   8.0% =  $80.00 [SYSTEM ROOT]

Total paid:        $180.00
Expected (18%):    $180.00
✅ COMMISSION CALCULATION CORRECT!
```

---

### `check_investment_bonus.py`

Показывает историю investment bonuses для пользователя.

```bash
python scripts/check_investment_bonus.py --user-id 5971989877
```

**Вывод:**
```
User: Зодд (ID: 5971989877)
Total invested: $5100
Current tier: $5000 (10%)
Next tier: $10000 (15%)

PURCHASE HISTORY:
1. $400.00 (total: $400.00) [No tier] → No bonus
2. $700.00 (total: $1100.00) [5% tier] → Bonus: $55.00
3. $4000.00 (total: $5100.00) [10% tier] → Bonus: $455.00

Total bonuses granted: $510.00
Expected total bonus: $510.00 (10% of $5100)
✅ Bonus calculation correct!
```

---

### `show_tree.py`

Визуализирует структуру MLM дерева.

```bash
# Показать полное дерево
python scripts/show_tree.py

# Показать поддерево
python scripts/show_tree.py --root-id 5971989877

# Только статистика
python scripts/show_tree.py --stats
```

---

## 🚀 Быстрый старт

### Тест 1: Дифференциальные комиссии

```bash
# Шаг 1: Создать структуру
python scripts/populate_test_differential_commission.py

# Шаг 2: Запустить бота
python jetup.py

# Шаг 3: В Telegram от Зодд сделать покупку $1000

# Шаг 4: Проверить результат
python scripts/check_commissions.py --last
```

---

### Тест 2: Investment Bonus

```bash
# Шаг 1: Создать структуру
python scripts/populate_test_investment_bonus.py

# Шаг 2: Запустить бота
python jetup.py

# Шаг 3: В Telegram от Зодд делаешь покупки:
#   - Первая: $400
#   - Вторая: $700 (получишь ~$55 опционов)
#   - Третья: $4000 (получишь ~$455 опционов)

# Шаг 4: После каждой покупки проверяешь
python scripts/check_investment_bonus.py --user-id 5971989877
```

---

### Тест 3: Referral Bonus

```bash
# Шаг 1: Создать структуру
python scripts/populate_test_referral_bonus.py

# Шаг 2: Запустить бота
python jetup.py

# Шаг 3: В Telegram от КИЛЛ сделать покупку ≥$5000

# Шаг 4: Проверить, что Зодд получил опционы
python scripts/check_commissions.py --last
python scripts/check_investment_bonus.py --user-id 5971989877
```

---

## 📊 Проверка через SQL (опционально)

### Комиссии по покупке

```sql
SELECT
    u.firstname,
    u.telegramID,
    u.rank,
    b.bonusAmount,
    b.bonusRate,
    b.commissionType
FROM bonus b
JOIN user u ON b.userID = u.userID
WHERE b.purchaseID = <purchase_id>
ORDER BY b.uplineLevel;
```

### Investment bonuses

```sql
SELECT
    bonusAmount,
    bonusRate,
    notes,
    createdAt
FROM bonus
WHERE userID = <user_id>
  AND commissionType = 'investment_package'
ORDER BY createdAt;
```

---

## ⚠️ Важные примечания

1. **Популяторы УДАЛЯЮТ БД!**
   - Все данные будут потеряны
   - Используй только на тестовых окружениях

2. **Живые аккаунты (⭐️)**
   - Это реальные Telegram аккаунты
   - Убедись, что у них правильные email и Telegram ID

3. **Google Sheets**
   - Популяторы импортируют проекты из Google Sheets
   - Убедись, что `Config.GOOGLE_SHEETS_URL` настроен

4. **Баланс**
   - Убедись, что у тестовых аккаунтов достаточно баланса для покупок
   - Популяторы дают стартовый баланс ($5000-$25000)

5. **Порядок покупок**
   - Для investment bonus важен порядок покупок
   - Делай покупки по инструкции популятора

---

## 🐛 Отладка

### Проверить chain integrity

```python
from mlm_system.utils.chain_walker import ChainWalker

session = get_session()
walker = ChainWalker(session)

if not walker.validate_default_referrer():
    print("❌ DEFAULT_REFERRER validation failed!")

orphans = walker.find_orphan_branches()
if orphans:
    print(f"❌ Found {len(orphans)} orphan users")
```

### Детальные логи

```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

---

## 📚 Дополнительные ресурсы

- **MLM_TESTING_SCHEMA.md** - полная схема тестирования со всеми сценариями
- **populate_test_data.py** - основной популятор в корне (для сложных структур)
- **mlm_system/** - исходный код MLM системы
- **models/** - модели данных

---

**Автор:** Claude Code
**Дата:** 2025-11-08
**Версия:** 2.0 (Live Testing Edition)
